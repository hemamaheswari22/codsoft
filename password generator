import random


lower = "abcdefghijklmnopqrstuvwxyz"

upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
numbers = "1234567890"
symbols = "!@#$%^&*()_+/"


print("welcome to passward generator")
length = int (input("enter no of digits of password:"))

n_lower=int(input("how many lower letters you want in your password?\n"))
n_upper=int(input("how many upper letters you want in your password?\n"))
n_upper=int(input("how many numbers you want in your password?\n"))
n_upper=int(input("how many symbols you want in your password?\n"))

all = lower + upper + numbers + symbols


passward = "" .join(random.sample(all,length))
print(passward)
